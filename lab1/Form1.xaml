<Window x:Class="CarApp.Form1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CarApp"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
        <ControlTemplate x:Key="validationTemplate">
            <DockPanel LastChildFill="True">
                <TextBlock DockPanel.Dock="Right" 
                    Foreground="Red"
                    FontSize="14pt" 
                    Margin="-15,0,0,0"
                    FontWeight="Bold"
                    Text="!"
                    ToolTip="{Binding [0].ErrorContent}"/>
                <Border BorderBrush="Red" BorderThickness="1">
                    <AdornedElementPlaceholder x:Name="controlWithError" />
                </Border>
            </DockPanel>
        </ControlTemplate>
    </Window.Resources>

    <Grid Name="Root">
        <Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

        <Grid.Resources>
            <DataTemplate x:Key="_datePicker">
                <DatePicker SelectedDate="{Binding}"/>
            </DataTemplate>
            <DataTemplate x:Key="datePicker">
                <StackPanel>
                    <ContentPresenter ContentTemplate="{StaticResource _datePicker}" 
                        Content="{Binding Path=(local:Properties.TemplateBinding),
                            RelativeSource={RelativeSource AncestorType={x:Type DataGridTemplateColumn}}}" />
                </StackPanel>
            </DataTemplate>
        </Grid.Resources>

        <DataGrid Name="CarDataGrid"
                  Grid.Row="0" Grid.Column="0"
                  AutoGenerateColumns="false">
            <DataGrid.Columns>
                <DataGridTemplateColumn
                    Header="ManufacturedOn"
                    local:Properties.TemplateBinding="{Binding Path=ManufacturedDate}"
                    CellTemplate="{StaticResource datePicker}"/>
            </DataGrid.Columns>
        </DataGrid>
        <!-- <Label Content="Color" />
        <xctk:ColorPicker Grid.Row="0" Grid.Column="1" />

        <Label Content="Has Owner" />
        <ToggleButton></ToggleButton>
        
        <Label Content="Owner Name" /> -->
    </Grid>
</Window>
